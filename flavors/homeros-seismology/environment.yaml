name: homeros-seismology
channels:
  - conda-forge
dependencies:
  # naavre-fl-*-cell-build
  - r-jsonlite=1.9.1
  - r-optparse=1.7.5
  # naavre-fl-*-jupyter
  - ipykernel=6.29.5
  - r-irkernel=1.3.2
  # user packages for all flavors
  - git=2.49.0
  - jq>=1.8
  - minio=7.2.15
  - pip=25.0.1
  - python>=3.10
  - r=4.4
  - r-secretsprovider
  - r-getpass
  - r-aws.s3
  # user packages for this flavor
  - numpy>=1.21.6,<2.0.0
  - pandas
  - matplotlib
  - cartopy
  - obspy
  - pytorch
  - pip:
    # pip package for all flavors
    - SecretsProvider
    # pip packages for this flavor
    - seisbench
    - pyocto
    - xdas
